package com.palette.palettepetsback.articleWrite.repository;

import com.palette.palettepetsback.articleWrite.entity.ArticleWrite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArticleWriteRepository extends JpaRepository<ArticleWrite,Long> {
}
